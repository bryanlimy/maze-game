package mazegame;

public interface Moveable {
	/**
	 * Move the object to the given row and column position.
	 * @param row the given row position
	 * @param col the given column position
	 */
	public void move(int row, int col);
}
